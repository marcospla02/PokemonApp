import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    background: {
      // paper: "#29a0d3",
      paper: "rgb(228, 233, 233)",
    },

    text: {
      primary: "#ffed00",
      secondary: "#fff200",
    },
  },
});
